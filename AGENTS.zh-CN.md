# LexPro AI 开发规则（中文说明）

[English source](AGENTS.md)

本文件是根目录 `AGENTS.md` 的中文镜像，帮助项目负责人了解 AI 在本项目中必须遵守的规则。AI 实际执行时仍以英文 `AGENTS.md` 为稳定入口，两份内容必须同步维护。

## 修改代码前必须执行

1. 阅读 `docs/IMPLEMENTATION_PLAN.md` 以及受影响模块的文档。
2. 涉及本机工具、数据库程序或环境配置时，先读 `docs/LOCAL_PATHS_AND_CONFIGURATION.md`，不要先扫描机器。
3. 在假设字段或行为前，检查真实前端代码和最终 V1/V2/V3 SQL。
4. 检查 Git 工作区并保留用户已有修改。
5. 编辑前说明任务范围、受影响文件和验证计划。

## 信息权威顺序

1. `docs/BUSINESS_RULES.md` 和 `docs/DECISIONS.md` 中已经确认的规则。
2. `DBM/` 中按 V1、V2、V3 顺序执行的最终数据库脚本。
3. `docs/API_CONVENTIONS.md` 和 OpenAPI 中已经确认的接口协议。
4. 当前应用代码和测试。
5. 前端 Mock 数据。Mock 只表达原型行为，不是权威业务数据。

不同来源发生冲突时，AI 必须停止并记录冲突，不能静默选择一个新规则。

## 固定技术基线

- Java 21 和 Spring Boot 3.5.x。
- Maven、MyBatis-Plus、Spring Security、PostgreSQL 和 Flyway。
- Vue 3、Vue Router、Element Plus 和 Vite。
- 后端按功能模块组织，例如 `auth`、`user`、`casework`、`dossier`、`report`。
- Spring Boot 保持模块化单体；只有开始文档和 AI 处理时才增加独立 Python 服务。

## 后端规则

- 遵守 `Controller -> Service -> Mapper` 边界。
- Controller 接收请求 DTO、返回响应 DTO，不直接返回数据库 Entity。
- 使用构造器注入，不使用字段注入。
- 事务放在 Service 方法上，查询使用 `readOnly = true`。
- 校验所有外部输入。
- 不泄漏密码哈希、身份证件号、内部存储路径、堆栈或密钥。
- 成功直接返回资源 JSON，错误使用标准 `ProblemDetail`，分页使用公共分页结构。
- 对外接口使用 `/api/v1`，时间使用带时区偏移的 ISO-8601。
- Service 层必须执行授权和案件数据范围检查，不能只依赖前端。
- 安全敏感操作和案件变更写入 `operation_log`。
- 未经确认，不引入 Redis、Kafka、Elasticsearch、微服务或其他基础设施。

## 数据库规则

- 当前基线是 V1 + V2 + V3 后的 32 张业务表；Flyway 元数据表属于基础设施，不计入该数量。
- V1/V2/V3 是一次性脚本，不能在已升级开发库中重复执行。
- 新需求不能修改已经执行的迁移文件，必须在基线后新增迁移。
- 未经用户明确批准以及备份/验证计划，不能执行结构变更、破坏性 SQL、删除数据或批量更新。
- PostgreSQL 使用 `snake_case`，Java 和 JSON 使用 `camelCase`。
- 用于组合外键的重复 `case_id` 是防止跨案件关联的完整性设计。
- `case_assignment.ended_at IS NULL` 表示当前有效分配。
- 需求未改变前，不增加公告、日程、通知、任务事件、多用户角色、多用户组织或案件 ACL 表。
- 确认 Embedding 模型前，不固定向量维度和向量索引。

## 安全和密钥

- 密码只使用 BCrypt 保存，不能存储或记录明文。
- 密钥从环境变量读取，不能提交真实 `.env` 或凭证。
- 系统不开放公众注册，账号由管理员创建。
- 当前每个用户只有一个主要组织和一个系统角色。
- 案件文件和个人身份信息都按敏感数据处理。

## 交付纪律

- 每次修改只覆盖已经批准的需求，不增加推测性抽象、功能、基础设施或顺手清理工作。
- 严格控制新增测试数量。只增加保护本次业务行为、授权边界、数据库/API 协议或可信回归风险所必需的最小测试集。
- 优先扩展已有测试，不轻易新建测试类。同一行为不得在 Controller、Service 和 Mapper 层重复测试，除非每一层确实存在不同的失败风险。
- 不为简单 DTO getter/setter、直接委托、框架/第三方库行为或已经被高价值行为测试覆盖的实现细节增加测试。
- 日常代码修改默认不产生新文档。只有计划状态、业务规则、技术决策、API 协议、环境变量或人工操作确实变化时，才更新对应的现有权威文档。
- 只有交接、运行或验收确有需要，并且现有文档没有合适位置时，才新建文档。每个重要模块最多保留一份简洁交接文档，不重复核心文档或 OpenAPI 已维护的内容。
- 未经用户明确要求，不编写里程碑总结、开发过程日志、重复架构说明或测试报告。

## 每个任务必须验证

- 使用能够证明修改行为的最小针对性测试集。只有修改影响公共/共享能力、进入里程碑验收或用户明确要求时，才运行全部 Maven 测试。
- 后端变更后构建后端，前端变更后构建前端。
- 数据库功能使用测试库验证，或只对开发数据执行只读检查。
- API 必须检查真实 HTTP 状态码和响应体。
- 检查响应中不存在敏感字段。
- 只有任务状态变化时才更新中英文实施计划。
- 核心英文文档变化时，在同一任务中更新对应 `docs/zh-CN` 文档。
- 报告验证内容、仍需人工完成的步骤和残余风险。

## 必须由用户批准的停止点

- 执行迁移或破坏性命令。
- 修改表结构或业务状态流转。
- 增加基础设施或外部服务。
- 将真实案件数据发送给外部 AI 服务商。
- 部署、发布或修改生产配置。
