# LexPro 后端开发启动前基线（历史）

> 梳理日期：2026-07-27  
> 梳理范围：当前仓库代码、前端 Mock 数据、`DBM` 数据库设计、`结构图展示.pptx`、`法律图.pptx`  
> 说明：本文描述的是仓库当前状态。数据库是否已经部署、是否存在库外后端代码，未连接运行环境验证；本文只确认仓库中已有 PostgreSQL 设计和建表脚本。

> 历史说明：本文保留后端启动前的调研结果，不再代表当前实现。当前状态以 `docs/IMPLEMENTATION_PLAN.md`、`docs/DECISIONS.md` 和最终 V1/V2/V3 的 32 表基线为准；M0、M1、M2 已完成。

## 1. 结论摘要

当前项目已经完成两类基础工作：

1. Vue 3 + Element Plus 的前端业务原型，主要页面、交互入口和展示字段已经具备。
2. PostgreSQL 15+ 的核心业务数据库设计，提供了可执行建表脚本，覆盖案件、卷宗、智能处理结果、案卡、报告、类案推荐和审计日志。

当前仓库**没有后端应用代码**。具体表现为：

- 没有后端工程目录、后端框架依赖、启动入口和配置文件。
- 没有 Controller/API、Service、Repository/DAO、ORM 实体或数据库连接配置。
- 没有登录认证、Token、RBAC、接口鉴权和路由守卫。
- 没有文件存储、MinerU/文档解析、模型调用、异步任务和导出实现。
- 没有数据库迁移工具、种子数据、后端测试、接口文档和部署配置。
- 前端没有 API 层，也没有 `fetch`/`axios` 请求；业务数据全部来自 `src/mock/*.json`。

因此，项目处于“**前端原型 + 数据库基线已完成，后端尚未开始**”的阶段。

## 2. 当前仓库结构

```text
lexpro/
├─ DBM/
│  ├─ DB_DESIGN_FINAL.md              # 数据库逻辑设计和开发约定
│  └─ lexpro_schema_postgresql.sql    # PostgreSQL 物理建表脚本
├─ src/
│  ├─ components/                     # Vue 公共组件和布局
│  ├─ mock/                           # 当前所有页面的数据来源
│  ├─ router/index.js                 # 前端路由
│  ├─ views/                          # 业务页面
│  ├─ App.vue
│  └─ main.js
├─ package.json                       # 仅包含前端依赖
├─ vite.config.js
├─ 结构图展示.pptx                    # 功能模块、流程、旧版 ER 图和界面参考
└─ 法律图.pptx                        # 项目技术路线和智能能力范围
```

前端技术栈：

- Vue 3.5
- Vue Router 4.4，使用 Hash 路由
- Element Plus 2.8
- Vite 6

当前 `package.json` 只有 `dev`、`build`、`preview` 三个前端命令，没有后端相关脚本。

## 3. 已有实现

### 3.1 前端页面和已表达的业务

| 页面/入口 | 当前已有表现 | 当前真实状态 |
|---|---|---|
| 登录 | 账号、密码、记住密码 | 当时使用硬编码演示凭据并直接跳转；M2 已替换为真实 JWT 登录 |
| 首页工作台 | 用户信息、应用中心、统计、待办案件、公告、日程 | 全部读取 Mock |
| 待办案件 | 案件列表、关键字过滤、状态、办理/查看入口 | 仅本地数组过滤，无增删改查 |
| 审查报告 | 卷宗预览、报告章节、步骤、保存/重生成/导出按钮 | 按钮只弹成功提示 |
| 法律要素识别 | 上传区、原文、识别结果 | 未上传文件，未调用模型 |
| 文书实体识别 | 上传区、原文、实体分类结果 | 未上传文件，未调用模型 |
| 案例摘要 | 原文、分段摘要、保存/重新生成 | 本地数据和提示信息 |
| 典型案例推送 | 多条件检索、争议焦点提取、结果、详情、引用、收藏 | 检索结果固定，引用/收藏未持久化 |
| 组织管理 | 部门、人员、负载、同步和新增入口 | 纯展示，数据库尚未完整覆盖 |
| 内容管理 | 知识条目、模板、案例、审核状态 | 纯展示，数据库只覆盖其中一部分 |
| 待办任务 | 任务统计、列表、动态、处理/转办/批量确认 | 纯展示，数据库没有通用任务表 |

补充现状：

- 路由没有登录态校验和权限控制。
- 顶部用户、待办数量、全局搜索均未连接后端。
- `recommendCases.json` 当前没有被页面引用；案例推荐页使用的是 `caseResults.json`。
- 页面字段使用前端驼峰命名，例如 `caseNo`、`caseType`；数据库使用 `snake_case`，后端需要统一 DTO 映射规则。

### 3.2 数据库物理实现

目标数据库为 PostgreSQL 15+，使用：

- `bigint GENERATED BY DEFAULT AS IDENTITY` 主键
- `timestamptz` 时间字段
- `jsonb` 结构化数据
- `pgvector` 的 `vector` 字段
- `varchar + CHECK` 状态约束
- `lexpro` 业务 Schema

SQL 脚本实际创建 21 张表：

| 领域 | 已有表 | 覆盖能力 |
|---|---|---|
| 用户与案件 | `app_user`、`case_record`、`case_party`、`case_assignment` | 用户主体、案件主数据、参与人、承办/复核分配历史 |
| 卷宗与解析 | `evidence_file`、`document_parse_result` | 文件元数据、解析状态、多版本解析结果 |
| 智能识别 | `entity_result`、`legal_element_result`、`case_summary` | 实体、法律要素、案件摘要结果 |
| 案卡 | `case_card_fill_task`、`case_card_task_source`、`case_card_field` | 回填任务、来源追踪、字段值和人工确认 |
| 审查报告 | `report_template`、`case_report`、`report_evidence`、`report_legal_element_result` | 模板、报告版本、证据和法律要素引用 |
| 类案推荐 | `typical_case`、`typical_case_content`、`case_recommendation`、`case_recommendation_item` | 案例库、正文、检索快照、排序和相似度 |
| 审计 | `operation_log` | 用户/案件操作审计和请求链路记录 |

数据库中已经处理的关键设计问题：

- 案件参与人独立建模，不依赖单个嫌疑人姓名字段。
- 卷宗解析、案件摘要、审查报告支持历史版本。
- 案卡字段可记录来源文档、原文位置、置信度和人工确认状态。
- 报告和证据、法律要素使用关系表，能够追踪报告依据。
- 真实案件与典型案例不做错误的一对一关系，推荐结果单独保存。
- 外键、唯一约束、状态约束、检查约束和常用查询索引较完整。
- 重要表包含 `updated_at` 自动更新时间触发器。

## 4. 后端需求范围

需求来源分为三层：PPT 中明确的系统功能、当前前端页面表达的交互、数据库已经确定的数据契约。

### 4.1 用户、认证与权限

明确需求：

- 用户注册、登录、退出、用户信息管理。
- 普通用户/管理员角色管理。
- Token 认证和接口鉴权。
- RBAC 权限控制和案件数据范围控制。
- 登录、关键业务操作写入审计日志。

后端需要实现：密码哈希、登录失败处理、Token 生命周期、当前用户接口、角色/权限检查、案件级数据权限。

V3 将权限规范化为 `auth_role`、`auth_permission`、`auth_role_permission`，并由 `app_user.role_id` 指向单一系统角色；当前需求中每个用户只有一个主角色，因此不增加用户角色关系表。

### 4.2 案件管理

明确需求：

- 案件创建、列表、详情、编辑和状态流转。
- 待办/在办/结办/归档分类。
- 参与人维护。
- 承办人、复核人分配及变更历史。
- 案件共享或案件访问控制。
- 截止日期、超期判断和案件数量/类型/进度统计。

数据库已覆盖主数据和人员分配，但状态流转规则、共享权限、软删除/归档策略仍需在服务层确定。

### 4.3 电子卷宗管理

明确需求：

- PDF、文本、音频等文件上传。
- 文件分类、标签、目录树、预览、更新和删除。
- 保存对象存储地址、大小、哈希和解析状态。
- 调用 MinerU 或其他解析服务，保存原文、结构化结果和解析版本。
- 解析失败重试、错误信息、进度查询。

数据库已覆盖文件元数据和解析结果；对象存储、文件目录/标签、预览转换、解析服务适配和异步执行尚未实现。

### 4.4 智能审查

明确需求：

- 文书实体识别。
- 法律要素识别。
- 案件摘要生成。
- 模型动态适配或智能路由。
- 结果人工复核、修订和确认。
- 保存模型、版本、输入来源和结构版本，保证结果可追溯。

后端不应让 HTTP 请求同步等待长时间模型任务。建议采用“创建任务 -> 异步执行 -> 查询状态/结果”的接口方式，并为外部模型服务定义稳定适配层。

### 4.5 案卡管理

明确需求：

- 从解析结果、实体、法律要素和摘要中自动提取案卡字段。
- 字段人工编辑、校验、确认、驳回和保存。
- 记录字段来源、原文位置、置信度和操作人。
- 案卡导出或向外部业务系统同步。

数据库已对回填任务、来源和字段做了较完整建模；字段模板、字段校验规则、同步目标和同步回执尚未定义。

### 4.6 审查报告管理

明确需求：

- 按模板生成案件基本信息、侦查意见、证据审查、审查认定和审查意见。
- 自动生成草稿，允许人工编辑和修改。
- 草稿、复核、定稿和历史版本管理。
- 重新生成时保留历史版本和引用来源。
- 导出 Word/PDF，案件办结后归档。

数据库已覆盖模板、报告、版本和部分引用关系；报告章节 JSON 协议、人工修改记录、复核意见、Word/PDF 生成和文件归档尚未实现。

### 4.7 类案检索与推荐

明确需求：

- 按标题、案由/罪名、法律条文、案件类型、案例等级、法院层级、地区、日期、程序和文书类型过滤。
- 根据案件事实提取争议焦点。
- 生成事实向量并进行相似案例检索和相似度计算。
- 展示推荐理由、相似度、案例详情。
- 将案例引用到审查报告，支持收藏。
- 保存每次推荐的输入快照、模型版本、结果和排序。

数据库覆盖案例库和推荐历史，但报告引用典型案例、用户收藏、混合检索策略、向量模型和索引尚未实现。

### 4.8 统计、组织、内容和待办

前端还表达了以下需求：

- 组织/部门/成员管理和人员同步。
- 通用待办任务、转办、批量确认、优先级、超期提醒和任务动态。
- 公告、日程安排和消息中心。
- 知识条目、规则库、内容分类、发布和审核。
- 案件数量、类型分布、办案进度和用户工作量统计。

数据库目前没有完整覆盖这些功能。它们不能只靠 `app_user.permissions_json` 或 `operation_log` 代替，需要先确认是否属于第一期后端范围。

## 5. 前端需求与数据库覆盖矩阵

| 功能 | 前端原型 | 数据库 | 后端服务 | 判断 |
|---|---:|---:|---:|---|
| 登录/用户 | 有 | 部分 | 无 | 可开始，但认证/RBAC 规则待定 |
| 案件管理 | 有 | 有 | 无 | 核心第一优先级 |
| 参与人/案件分配 | 页面未完整展开 | 有 | 无 | 应随案件详情实现 |
| 卷宗管理 | 报告页有局部展示 | 有 | 无 | 文件存储和解析方案待定 |
| 实体识别 | 有 | 有 | 无 | 需要模型服务契约 |
| 法律要素识别 | 有 | 有 | 无 | 需要模型服务契约和人工确认 |
| 案例摘要 | 有 | 有 | 无 | 需要版本/重新生成规则 |
| 案卡 | 结构图有，当前路由无独立页 | 有 | 无 | 数据库已准备，前端尚未完整实现 |
| 审查报告 | 有 | 有 | 无 | 需要章节协议、复核和导出 |
| 类案推荐 | 有 | 有 | 无 | 向量维度和检索策略待定 |
| 统计分析 | 有 | 可聚合部分数据 | 无 | 先用查询聚合，后续再考虑统计表 |
| 组织管理 | 有 | 不完整 | 无 | 缺部门/成员关系模型 |
| 内容管理 | 有 | 部分 | 无 | 模板/案例已覆盖，通用内容未覆盖 |
| 待办任务 | 有 | 无 | 无 | 缺任务、流转、提醒模型 |
| 公告/日程/消息 | 有 | 无 | 无 | 需确认是否进入本期 |

## 6. 开发前必须处理的缺口

### 6.1 需要产品或技术确认

1. 后端语言和框架尚未确定，仓库中没有任何偏好证据。
2. 登录采用 JWT、服务端 Session，还是对接统一身份认证。
3. 第一阶段是否包含用户注册；政法内网系统通常更可能由管理员建号或同步组织账号。
4. `prosecutor_id` 的真实语义是承办人还是检察官，是否统一改用 `case_assignment`。
5. 案件编号是否全局唯一，案件允许哪些状态转换。
6. 身份证号等敏感字段的加密、脱敏、查询和密钥管理方式。
7. 文件保存到本地、MinIO，还是已有对象存储；允许的类型、大小和保留期限。
8. MinerU 和大模型服务的地址、鉴权、请求协议、超时、重试和并发限制。
9. AI 结果是否允许人工覆盖；覆盖后是否保留原结果和修改记录。
10. 报告模板、章节 JSON、Word/PDF 样式和定稿/撤回规则。
11. Embedding 模型、固定向量维度、距离算法和混合检索排序规则。
12. 组织、内容、待办、公告和日程是否属于第一期范围。

### 6.2 V1 缺口的迁移处理

1. V2 将提示快照、生成参数、耗时、Token 和请求 ID 保存在对应智能结果、案卡、报告和推荐记录中，不再增加通用模型调用表。
2. V2 为实体结果补充人工确认，为摘要补充明确版本号，为报告补充生成追溯和当前版本标记。
3. V2 增加案例收藏、报告案例引用、卷宗目录和规范化文件标签关系。
4. V3 增加组织、单角色 RBAC、知识内容和人工待办；待办时间线复用 `operation_log`。
5. 公告、日程和通知属于工作台展示能力，不进入当前核心业务数据库。
6. 向量字段仍是不限定维度的 `vector`，也未创建 HNSW 索引；必须在选定 Embedding 模型后单独迁移。

### 6.3 工程基础缺口

- 环境配置和密钥管理。
- 数据库迁移版本管理，而不是只维护一份全量 SQL。
- 统一响应结构、错误码、分页、排序和筛选协议。
- OpenAPI/Swagger 接口文档。
- 参数校验、异常处理、请求 ID 和结构化日志。
- 单元测试、数据库集成测试和 API 集成测试。
- 文件安全检查、访问签名、敏感数据脱敏和审计。
- Docker/部署配置、健康检查和可观测性。

## 7. 建议的后端模块边界

无论最终选择 Java、Python、Node.js 还是其他技术栈，建议按业务边界组织，而不是按数据库表逐表堆接口：

```text
backend/
├─ auth/              # 登录、Token、当前用户、权限
├─ users/             # 用户和组织
├─ cases/             # 案件、参与人、分配、状态流转
├─ dossiers/          # 上传、对象存储、目录、解析
├─ intelligence/      # 实体、法律要素、摘要、模型适配
├─ case-cards/        # 案卡任务、字段、确认、同步
├─ reports/           # 模板、生成、编辑、复核、定稿、导出
├─ recommendations/   # 案例库、检索、向量推荐、引用、收藏
├─ tasks/             # 通用异步任务和业务待办
├─ statistics/        # 工作台和统计查询
├─ audit/             # 操作日志
└─ common/            # 配置、错误、分页、日志、安全、存储适配
```

建议保持以下调用方向：

```text
HTTP API
  -> 应用服务（用例、事务、权限）
  -> 领域/业务规则
  -> Repository、对象存储、任务队列、MinerU、模型服务、导出服务
```

外部解析和模型调用要通过适配器隔离，业务服务不直接依赖某个厂商的请求/响应结构。

## 8. 建议开发顺序

### 阶段 0：确定契约和工程骨架

- 确定后端技术栈、包管理、配置和目录规范。
- 引入数据库迁移工具，并把现有 SQL 作为基线迁移。
- 定义统一响应、错误码、分页、时间格式和 DTO 命名转换。
- 生成 OpenAPI，建立最小单元测试和集成测试环境。
- 明确认证方式、对象存储、MinerU、模型服务和任务执行方案。

### 阶段 1：打通最小业务闭环

建议先完成：

```text
登录 -> 当前用户 -> 案件列表/详情/创建
     -> 参与人和承办人 -> 文件上传/列表/下载
     -> 操作日志
```

第一批接口建议：

```text
POST   /api/v1/auth/login
POST   /api/v1/auth/logout
GET    /api/v1/auth/me

GET    /api/v1/cases
POST   /api/v1/cases
GET    /api/v1/cases/{caseId}
PATCH  /api/v1/cases/{caseId}
POST   /api/v1/cases/{caseId}/status-transitions

GET    /api/v1/cases/{caseId}/parties
POST   /api/v1/cases/{caseId}/parties
PATCH  /api/v1/cases/{caseId}/parties/{partyId}

GET    /api/v1/cases/{caseId}/assignments
POST   /api/v1/cases/{caseId}/assignments

GET    /api/v1/cases/{caseId}/files
POST   /api/v1/cases/{caseId}/files
GET    /api/v1/files/{fileId}
GET    /api/v1/files/{fileId}/download
```

阶段 1 完成后，前端登录、待办案件和卷宗基础数据应完全脱离 Mock。

### 阶段 2：文档解析和智能结果

- 文件触发解析、状态查询、失败重试和多版本结果。
- 实体识别、法律要素识别、摘要生成。
- 人工确认和历史结果查询。
- 通用异步任务、幂等、超时、重试和模型调用审计。

### 阶段 3：案卡和审查报告

- 案卡字段生成、来源追踪、人工确认和保存。
- 报告模板、草稿生成、编辑、复核、定稿和历史版本。
- 典型案例引用、证据引用和法律要素引用。
- Word/PDF 导出及归档。

### 阶段 4：类案检索

- 案例导入和内容解析。
- Embedding 生成、固定向量维度和 HNSW 索引。
- 结构化过滤 + 全文检索 + 向量检索的混合召回。
- 推荐历史、理由、收藏和报告引用。

### 阶段 5：管理和统计

- 组织、用户同步、可配置 RBAC。
- 通用待办、转办、提醒、消息。
- 内容审核和发布。
- 工作台统计、用户工作量和可视化数据接口。

## 9. 第一阶段完成标准

后端第一阶段不能只以“接口能返回数据”为完成标准，至少应满足：

- 登录后才能访问业务接口，越权访问有明确拒绝结果。
- 案件列表支持分页、关键字、状态、类型、承办人和时间筛选。
- 案件详情返回参与人、当前人员分配和卷宗摘要，避免前端发起大量零散请求。
- 案件状态只能按允许路径转换，并记录操作日志。
- 文件上传校验类型、大小和哈希，数据库只保存内部对象 Key/URI。
- 身份证号等敏感字段按规则加密存储、脱敏返回。
- 所有写接口有参数校验、事务边界和可追踪的错误码。
- OpenAPI 可访问，核心 Service 和 API 有自动化测试。
- 前端对应页面已移除相关 Mock 引用并完成联调。

## 10. 建议下一步

开始写业务代码前，先完成两个短任务：

1. 确定后端技术栈和认证、对象存储、异步任务、模型服务四项基础方案。
2. 对第 6.1 节的业务规则做一次确认，并决定组织/待办/内容/公告是否进入第一期。

确认后，直接建立后端工程骨架，实现“认证 + 案件 + 参与人/分配 + 卷宗 + 审计”的第一阶段闭环。这个顺序与现有数据库依赖关系一致，也能最快把当前前端从 Mock 数据切换到真实接口。
