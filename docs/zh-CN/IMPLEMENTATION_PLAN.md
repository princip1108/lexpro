# LexPro 实施计划

[English](../IMPLEMENTATION_PLAN.md)

> 更新日期：2026-09-03
> 交付方式：使用 AI 辅助完成小型纵向业务切片，每个任务独立测试、独立验收。

## 当前基线

- [x] Vue 3 前端原型和 Mock 数据。
- [x] PostgreSQL V1/V2/V3 数据库设计，最终 32 张业务表。
- [x] Java 21 + Spring Boot 3.5.16 后端工程。
- [x] PostgreSQL 连接和数据库健康检查接口。
- [x] MyBatis-Plus 集成。
- [x] 用户只读列表，默认不查询和返回密码。
- [x] Controller 测试和应用上下文测试。
- [x] JWT 认证、实时账号/权限检查和 BCrypt 凭据处理。
- [x] 管理员用户维护以及组织/RBAC 查询。
- [x] 前端真实登录、会话过期处理和路由保护。

## M0 - 仓库和项目治理

- [x] M0-S1 在 LexPro 根目录初始化统一 Git 仓库。
- [x] M0-S2 添加根目录忽略规则和项目 `AGENTS.md`。
- [x] M0-S3 添加施工、API、业务、数据库和技术决策文档。
- [x] M0-S4 记录后端启动方法和必须的环境变量。
- [x] M0-S5 引入 Flyway，但不能重新执行现有数据库的 V1/V2/V3。
  - [x] 添加 Flyway 依赖和默认安全配置。
  - [x] 打包可追溯且无手工事务外壳的 V1/V2/V3 迁移资源。
  - [x] 添加迁移资源完整性测试。
  - [x] 在可丢弃数据库执行 V1/V2/V3 并验证最终 32 张业务表。
  - [x] 备份、验证并明确批准将现有开发数据库登记为 V3 基线。

## M1 - 后端公共规范

- [x] M1-S1 使用标准 `ProblemDetail` 和全局异常处理。
- [x] M1-S2 为请求 DTO 添加 Bean Validation，并返回字段错误。
- [x] M1-S3 建立统一分页请求和响应协议。
- [x] M1-S4 集成 SpringDoc OpenAPI 和本地 Swagger UI。
- [x] M1-S5 配置明确的 Vue 开发环境跨域规则。
- [x] M1-S6 增加请求 ID、结构化日志和审计辅助组件。
- [x] M1-S7 完成本里程碑代码审查和回归测试。

## M2 - 认证、用户和组织

- [x] M2-S1 用户详情 DTO 和查询服务。
- [x] M2-S2 安全地初始化开发环境管理员。
- [x] M2-S3 BCrypt 密码处理和登录凭证查询。
- [x] M2-S4 登录接口和 JWT 签发。
- [x] M2-S5 Spring Security JWT 认证及认证/授权错误处理。
- [x] M2-S6 当前用户和退出接口。
- [x] M2-S7 用户创建、禁用和密码重置管理。
- [x] M2-S8 组织树、角色和权限查询。
- [x] M2-S9 替换前端登录 Mock 并增加路由保护。
- [x] M2-S10 认证和安全专项审查。

## M3 - 案件流程

- [x] 案件分页、筛选、创建、详情和修改。
- [ ] 明确的案件状态流转规则。
  - 需先确认状态流转矩阵以及哪些角色可以执行流转。
- [x] 参与人及安全的敏感身份信息处理。
  - 身份证原值录入、加密、哈希和精确查询需等身份信息策略确认后再做。
- [x] 承办人、复核人和协办人的分配历史。
- [x] 案件级授权、期限/超期计算和审计。
- [x] 替换待办案件和首页第一批 Mock。

## M4 - 卷宗管理

- [x] 文件存储抽象和开发环境本地存储。
- [x] 安全上传、哈希、元数据、列表和下载。
- [x] 目录树、标签、软删除和案件权限。
- [ ] 仅在确认存储需求后添加 MinIO 适配器。

## M5 - 文档和智能处理

- [x] 文档解析协议、异步执行、重试和解析版本。
  - 本地 UTF-8 文本解析继续可用；MinerU PDF 解析及图片经 Java 后端调用 MinerU、中文解析结果落库的真实联调已通过，使用虚构测试材料。隧道恢复后，DOCX 文字、表格提取、内嵌图片调用真实 MinerU、结果落库及 UTF-16 定位均通过真实 HTTP 验收；损坏 DOCX 和旧版 DOC 的安全错误码已验证。旧版 DOC 需另存为 DOCX/PDF，更广泛语料验收仍待完成。
- [x] 实体识别、原始/最终结果和人工确认。
  - 内部 LexPro_8B 路由已限制为六类实体，并校验准确的块内/全文 UTF-16 位置；旧 DeepSeek 路由仍须单独明确批准外部数据传输。
- [x] 法律要素识别和证据来源追溯。
  - 模型输出和第一次人工确认都必须包含准确原文引用；可选偏移量会与解析文本进行校验。
- [x] 案件摘要版本和人工确认。
  - `FACT`、`PROCESS`、`CONCLUSION` 和 `FULL` 摘要使用显式选择的同案件解析来源；只有生成成功后才切换当前版本。

## M6 - 案卡和审查报告

- [x] 案卡生成、类型化来源、字段和人工确认。
- [x] 报告模板、生成、编辑、复核和定稿。
  - [x] 模板版本创建/查询、报告异步生成和草稿乐观锁编辑。
  - [x] 按已批准规则完成模板启用、报告送审/退回/定稿及审计。
- [x] 证据、法律要素和典型案例引用。
- [x] 使用统一司法文书版式导出可编辑 Word 和自动分页 PDF。

## M7 - 典型案例推荐

- [x] 定义 Java 到 Python 的内部检索协议和失败语义。
- [x] 完成典型案例导入、规范化、结构化筛选和全文召回。
- [x] 确认使用本地 `BAAI/bge-m3`、1024 维和余弦距离。
- [x] 新增只向前执行的 V4 向量迁移和余弦 HNSW 索引。
- [x] 在 Python 中实现向量召回、RRF 融合、重排和推荐理由。
- [x] Java 执行案件授权、调用 Python，并负责推荐历史、收藏持久化和审计。
- [x] 增加可配置超时、一次有限重试、词法降级、可追溯性和必要检索质量测试。
- [x] 增加可选的合作方托管提供方，使用签名的两阶段分析、严格字段映射和事务化本地镜像，并禁止从本地提供方自动切换或降级。
- [x] 新增并执行只向前的 V6，用于保存 `legal_llm` 案例元数据；开放扩展案例库筛选且不展示类别数量。
- [x] 完成通过正式应用接口迁移 `legal_llm` SQLite 案例库和演示案件所需的代码与工具。
  - 只读预检确认 6786 条案例均可转换且具有稳定唯一键；本地已迁移 5 个演示案件和现有 4 份案卷。
  - 典型案例全量向量化和导入明确留待服务器协作，本地开发机无需完整部署 BGE-M3；另有 1 份源 PDF 本地缺失。

## M8 - MCP 集成

- [ ] 确认具体 MCP 客户端，并完成真实客户端验收。
  - [x] 保留 Java 进程内实现、`/mcp` 无状态 Streamable HTTP，并以 Spring Boot 为信任边界。
- [x] 将当前网页登录 JWT 依赖替换为外部 MCP 专用服务令牌认证，并按客户端记录审计身份。
- [x] 将旧六工具目录替换为法律要素识别、文书实体识别、案件摘要和只读合作方典型案例推荐工具。
- [x] 复用三个已校验的 DeepSeek-compatible 客户端，使用严格输入/输出 Schema，且不暴露原始 prompt 或供应商响应。
- [x] 增加有界的客户端级并发/速率限制、令牌轮换/撤销、适合部署的超时和必要安全测试。
- [ ] 在 HTTPS 后完成协议、授权、隔离以及来自外部项目的真实客户端验收。

## M9 - 工作台和交付

- [x] 本地语言模型调用统一至 LexPro_8B，MinerU 继续负责解析。使用虚构材料的实体识别、法律要素、摘要、案卡和报告真实联调及落库全部通过。Java 生成接口固定使用 HTTP/1.1，解决 vLLM 对 h2c 升级误报请求体缺失的问题；针对性本地 HTTP 回归测试通过。长上下文及更广泛生成质量验收仍待完成。

- [ ] 原始 legal_llm 界面差异验收：具名要素结果行、推荐双栏、案卡确认和模型配置已落地代码，浏览器及服务器验收待完成。
- [x] V7 模型 API 配置迁移经明确批准、备份校验后通过 Flyway 执行；当前 33 张业务表，V1—V6 未改动或重跑。
- [x] 本地真实 HTTP 验收通过模型配置增删改、生效切换和角色权限校验；虚构材料的实体识别、法律要素、摘要、案卡和报告均成功生成并落库。7 个实体位置和 3 条证据引文的 UTF-16 坐标与原文一致，包含补充平面 Unicode 字符。
- [ ] 完成典型案例推荐联调：转发端口 8000 的合作方服务仍不可达；案例库浏览已单独验证。原版 UI 的视觉验收仍待完成。
- [x] 本地验收数据：导入 100 条 SQLite 元数据样本、不生成向量；5 个演示案件分配至管理员、检察官、审查人员账号，保留原账号及密码。

- [x] 知识内容和人工待办。
  - 知识和任务状态流转命令仍需等待流转矩阵批准。
- [x] 首页和统计接口。
- [x] 删除业务范围内剩余的前端 Mock。
- [ ] 安全、性能、备份恢复和部署验证。
  - [x] 完成 M9 访问边界复核、聚焦服务测试、后端打包和前端生产构建。
  - [x] 使用真实开发数据库验收管理员核心流程、M9 工作台、M4 非删除流程和 M5 本地解析。
  - [x] 使用虚构普通用户和真实开发数据库验收角色权限及案件分配的授权、撤权边界。
  - [x] 建立本地 HTTP 冒烟基线：40 次串行和 60 次并发受保护查询全部返回 200，本地 P95 为 110.01 ms。
  - [ ] 验收删除生命周期、外部 AI 正向流程和生产规模性能，并演练备份恢复和验证部署。

## 单个任务的完成标准

1. 实现前明确写出行为要求和不包含的范围。
2. 代码遵循模块边界和安全规则。
3. 相关自动化测试通过。
4. 受影响的应用能够成功构建。
5. 真实接口或界面可以人工验证。
6. 中英文文档和本计划同步更新。
7. Git 差异中只有本任务的预期修改。
