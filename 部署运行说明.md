# LexPro 部署、运行与验收说明

本文档用于在一台新的开发或演示机器上部署本仓库。仓库包含完整 Vue 前端、Spring Boot 后端、PostgreSQL V1–V7 建表迁移、MinerU/LexPro 适配器和必要配置模板；不包含模型权重、真实案件文件、本机数据库、密码、令牌和运行日志。

## 1. 组件与端口

| 组件 | 默认地址 | 是否必需 | 说明 |
| --- | --- | --- | --- |
| Vue 前端 | `http://127.0.0.1:5173` | 是 | 用户界面 |
| Spring Boot 后端 | `http://127.0.0.1:8080` | 是 | 业务 API、权限、数据持久化 |
| PostgreSQL | `127.0.0.1:5432/lexpro` | 是 | 业务数据库，schema 为 `lexpro` |
| Python AI 适配器 | `http://127.0.0.1:8020` | 智能功能必需 | Java 与 MinerU/LexPro 之间的内部接口 |
| LexPro_8B | `http://127.0.0.1:8001` | 智能功能必需 | OpenAI 兼容 vLLM 服务 |
| MinerU | `http://127.0.0.1:13456` | 文件解析必需 | PDF、图片及 DOCX 内嵌图片解析 |
| 典型案例服务 | `http://127.0.0.1:8000` | 可选 | 使用合作方典型案例推送时启用 |

前端只访问 8080；业务后端访问 8020、8001 和可选的 8000；8020 再访问 8001 与 13456。模型可运行在服务器上，通过 SSH 隧道映射到上述本地端口，无需把模型权重提交到 GitHub。

## 2. 环境要求

- Git。
- JDK 21，`java -version` 应显示 21。
- Node.js 20 或更高版本，以及 npm。
- PostgreSQL 15 或更高版本，并安装与 PostgreSQL 版本匹配的 `pgvector` 扩展。
- Python 3.11 或 3.12，仅用于 `backend/ai-service`。
- 可访问已部署的 LexPro_8B 与 MinerU；只验证普通页面时可暂不连接模型。

拉取代码：

```powershell
git clone https://github.com/princip1108/lexpro.git
cd lexpro
```

## 3. 创建数据库和全部表

### 3.1 创建空数据库

以 PostgreSQL 管理员执行：

```sql
CREATE DATABASE lexpro
    WITH ENCODING 'UTF8'
    TEMPLATE template0;
```

连接到刚创建的 `lexpro` 数据库，再执行：

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

不要先手工执行 `DBM`，再让 Flyway 重复执行迁移。全新安装只使用下面的 Flyway 方式。仓库内的最终迁移位于：

```text
backend/lexpro-backend/src/main/resources/db/migration/
  V1__create_core_schema.sql
  V2__upgrade_core_traceability.sql
  V3__add_workspace_and_rbac.sql
  V4__configure_typical_case_vectors.sql
  V5__add_partner_typical_case_fields.sql
  V6__add_legal_llm_typical_case_metadata.sql
  V7__model_api_configuration.sql
```

V1–V7 执行完成后应有 33 张业务表，另有 Flyway 自己的元数据表。V1–V3 是 32 张表的基础版本，V7 新增模型配置表。

### 3.2 首次启动所需环境变量

以下命令仅作用于当前 PowerShell 窗口。请替换数据库密码和随机密钥；不要把真实值写回仓库的 `.env.example`。

```powershell
$env:LEXPRO_DB_URL='jdbc:postgresql://127.0.0.1:5432/lexpro?currentSchema=lexpro'
$env:LEXPRO_DB_USERNAME='postgres'
$env:LEXPRO_DB_PASSWORD='<数据库密码>'
$env:LEXPRO_JWT_SECRET='<至少32字节的随机字符串>'
$env:LEXPRO_FLYWAY_ENABLED='true'
$env:LEXPRO_FLYWAY_BASELINE_ON_MIGRATE='false'

$env:LEXPRO_BOOTSTRAP_ADMIN_ENABLED='true'
$env:LEXPRO_BOOTSTRAP_ADMIN_USERNAME='demo_admin'
$env:LEXPRO_BOOTSTRAP_ADMIN_PASSWORD='LexProDemo2026!'
$env:LEXPRO_BOOTSTRAP_ADMIN_REAL_NAME='管理员'
$env:LEXPRO_BOOTSTRAP_ORGANIZATION_CODE='LEXPRO'
$env:LEXPRO_BOOTSTRAP_ORGANIZATION_NAME='LexPro'
```

随后首次启动后端：

```powershell
cd backend\lexpro-backend
.\mvnw.cmd spring-boot:run
```

Linux/macOS 使用：

```bash
cd backend/lexpro-backend
chmod +x mvnw
./mvnw spring-boot:run
```

看到应用成功启动后，另开终端验证：

```powershell
curl.exe http://127.0.0.1:8080/api/health/database
```

预期数据库状态为 `UP`、`databaseName` 为 `lexpro`、`tableCount` 为 `33`。首次成功后停止后端，并在以后启动的终端中设置：

```powershell
$env:LEXPRO_FLYWAY_ENABLED='false'
$env:LEXPRO_BOOTSTRAP_ADMIN_ENABLED='false'
```

已存在数据库不能直接设置 `baseline-on-migrate=true` 或重跑 V1–V3；必须先备份并核对 `flyway_schema_history`。

### 3.3 创建演示角色账号

首次引导会安全地创建管理员账号，密码由后端保存为 BCrypt 哈希：

| 权限身份 | 用户名 | 演示密码 |
| --- | --- | --- |
| 管理员 | `demo_admin` | `LexProDemo2026!` |
| 检察官 | `demo_prosecutor` | `LexProDemo2026!` |
| 审查人员 | `demo_reviewer` | `LexProDemo2026!` |

管理员首次登录后，在“系统管理 → 用户与组织”中创建后两个账号，并分别设置检察官、审查人员角色。若新库中只有系统自带的 `ADMIN`、`USER` 角色，可先在该页面按需要创建两个角色，并授予案件读取/编辑、卷宗管理、智能处理、报告管理、典型案例和任务管理权限。不要通过 SQL 保存明文密码。正式部署必须更换三者密码，登录页的演示凭据也只适用于演示环境。

## 4. 连接服务器上的 LexPro 与 MinerU

如果两个模型在同一服务器上，可在本地单独打开一个终端并保持运行：

```powershell
ssh -N -p <SSH端口> `
  -L 8001:127.0.0.1:8001 `
  -L 13456:127.0.0.1:13456 `
  <用户名>@<模型服务器IP>
```

如果服务在不同服务器，分别建立两个隧道：

```powershell
ssh -N -p <SSH端口> -L 8001:127.0.0.1:8001 <用户名>@<LexPro服务器IP>
ssh -N -p <SSH端口> -L 13456:127.0.0.1:13456 <用户名>@<MinerU服务器IP>
```

如合作方典型案例服务也需要转发，再建立：

```powershell
ssh -N -p <SSH端口> -L 8000:127.0.0.1:<服务器上的典型案例端口> <用户名>@<典型案例服务器IP>
```

验证端口：

```powershell
curl.exe http://127.0.0.1:8001/v1/models
curl.exe http://127.0.0.1:13456/health
```

第一条应返回模型 `LexPro_8B`，第二条应返回 MinerU 的 `status: ok`。SSH 隧道断开后智能功能会报 `AI_PROVIDER_UNAVAILABLE` 或 `MINERU_SERVICE_REJECTED_REQUEST`，此时先恢复隧道再重试。

## 5. 启动 Python AI 适配器

打开新的 PowerShell：

```powershell
cd backend\ai-service
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt

$env:LEXPRO_AI_SERVICE_INTERNAL_TOKEN='<至少32字节的内部随机令牌>'
$env:LEXPRO_AI_SERVICE_MINERU_BASE_URL='http://127.0.0.1:13456'
$env:LEXPRO_AI_SERVICE_LEXPRO_BASE_URL='http://127.0.0.1:8001'
$env:LEXPRO_AI_SERVICE_LEXPRO_MODEL_NAME='LexPro_8B'

.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8020
```

Linux/macOS 将虚拟环境命令替换为 `.venv/bin/python`。内部令牌必须与下一节后端配置中的 `LEXPRO_AI_SERVICE_INTERNAL_TOKEN` 完全一致。

## 6. 正常启动 Spring Boot 后端

打开新的 PowerShell，重新设置数据库、JWT、模型和适配器环境变量：

```powershell
$env:LEXPRO_DB_URL='jdbc:postgresql://127.0.0.1:5432/lexpro?currentSchema=lexpro'
$env:LEXPRO_DB_USERNAME='postgres'
$env:LEXPRO_DB_PASSWORD='<数据库密码>'
$env:LEXPRO_JWT_SECRET='<至少32字节的随机字符串>'
$env:LEXPRO_FLYWAY_ENABLED='false'
$env:LEXPRO_BOOTSTRAP_ADMIN_ENABLED='false'

$env:LEXPRO_AI_ENABLED='true'
$env:LEXPRO_AI_BASE_URL='http://127.0.0.1:8001/v1'
$env:LEXPRO_AI_API_KEY='EMPTY'
$env:LEXPRO_AI_MODEL='LexPro_8B'
$env:LEXPRO_AI_ALLOW_EXTERNAL_CASE_DATA='false'
$env:LEXPRO_AI_READ_TIMEOUT='PT15M'
$env:LEXPRO_AI_MAX_OUTPUT_TOKENS='1536'
$env:LEXPRO_AI_ENABLE_THINKING='false'

$env:LEXPRO_AI_SERVICE_ENABLED='true'
$env:LEXPRO_AI_SERVICE_BASE_URL='http://127.0.0.1:8020'
$env:LEXPRO_AI_SERVICE_INTERNAL_TOKEN='<与8020适配器相同的令牌>'

$env:LEXPRO_TYPICAL_CASE_PROVIDER='PARTNER'
$env:LEXPRO_PARTNER_TYPICAL_CASE_BASE_URL='http://127.0.0.1:8000'
$env:LEXPRO_PARTNER_TYPICAL_CASE_ALLOW_CASE_DATA='false'

cd backend\lexpro-backend
.\mvnw.cmd spring-boot:run
```

如果暂时不验收典型案例推送，把 `LEXPRO_TYPICAL_CASE_PROVIDER` 改为 `LOCAL`；这不影响文件解析、实体识别、法律要素、案卡和审查报告。

模型配置页面需要持久化 API 密钥时，再设置下列变量；普通 LexPro 联调不依赖该页面：

```powershell
$env:LEXPRO_MODEL_CONFIG_ENABLED='true'
$env:LEXPRO_MODEL_CONFIG_MASTER_KEY='<至少32字节、仅本机保存的加密密钥>'
$env:LEXPRO_MODEL_CONFIG_ALLOWED_URLS='http://127.0.0.1:8001/v1'
```

## 7. 启动 Vue 前端

打开新的 PowerShell：

```powershell
cd <仓库目录>
Copy-Item .env.example .env.local
npm ci
npm run dev
```

`.env.local` 的推荐内容：

```dotenv
VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_TYPICAL_CASE_PROVIDER=PARTNER
```

该 provider 必须与后端 `LEXPRO_TYPICAL_CASE_PROVIDER` 一致。浏览器访问 `http://127.0.0.1:5173/#/login`。生产构建使用：

```powershell
npm run build
```

产物生成到 `dist/`，该目录不提交 Git。

## 8. 推荐启动顺序

1. 启动 PostgreSQL。
2. 建立并保持 SSH 隧道。
3. 检查 8001 和 13456 健康状态。
4. 启动 Python AI 适配器 8020。
5. 启动 Spring Boot 后端 8080。
6. 启动 Vue 前端 5173。
7. 登录并开始验收。

出现 `Port 8080 was already in use` 时，说明已有后端实例正在运行，不要重复启动。先用 `Get-NetTCPConnection -LocalPort 8080` 查明进程；只有确认是本项目的旧实例后才停止它。

## 9. 交付验收

按以下顺序验收，可快速定位问题所在层：

1. `GET /api/health/database` 返回 `UP` 且 33 张业务表。
2. `GET http://127.0.0.1:8001/v1/models` 返回 `LexPro_8B`。
3. `GET http://127.0.0.1:13456/health` 返回 `ok`。
4. 使用管理员账号登录，确认工作台、用户与组织、模型配置和操作日志可访问。
5. 创建演示案件，上传 TXT、纯文字 DOCX、含图片 DOCX、PDF 和 JPG/PNG 测试材料。
6. 等待解析完成，检查实体识别六类结果、高亮定位和法律要素证据定位。
7. 执行案卡回填，确认字段有来源文本并可人工确认。
8. 在报告模板页创建并激活模板，然后生成审查报告，确认中文内容、编辑和导出可用。
9. 典型案例服务已连通 8000 时，再验收日期区间、查询表单、结果页与收藏；未部署该可选服务时应单独记录，不判定 LexPro/MinerU 失败。
10. 分别用检察官和审查人员账号登录，检查菜单权限和案件级访问边界。

仓库根目录的 `演示用_虚构合同诈骗案审查材料.txt` 可作为不含真实个人数据的基础测试材料。不得把真实案件材料提交到 GitHub。

## 10. GitHub 交付内容与排除项

必须保留：

- `src/`、`index.html`、`package.json`、`package-lock.json`、`vite.config.js`：完整 Vue 前端。
- `backend/lexpro-backend/`：完整 Spring Boot 后端、Maven Wrapper、测试和 V1–V7。
- `backend/ai-service/`：MinerU/LexPro Python 适配器和测试。
- `DBM/`：已评审数据库设计参考；全新部署仍以 Flyway 为准。
- `docs/`：业务规则、API 约定、决策和实施状态。
- `tools/`：脱敏迁移/验收辅助工具。
- `typical_case_service/`：本地典型案例服务源码；不包含模型目录。
- `xunwenbilu6.0/code/`、相关 README 和模型校验清单：服务器侧服务代码；不包含模型权重和本地部署配置。
- `.env.example` 与各服务的 `.env.example`：配置字段模板，不含真实值。

严禁提交：

- 任意 `.env`、数据库密码、JWT 密钥、API key、内部令牌、SSH 私钥。
- PostgreSQL dump、SQLite 数据库、`legal.db`、真实案件或个人身份数据。
- LexPro、MinerU、BGE-M3 等模型权重和缓存。
- `node_modules/`、`dist/`、`target/`、`.venv/`、日志、上传文件和运行时目录。
- 本机绝对路径配置、IDE 配置及合作方未授权发布的文件。

提交前至少执行：

```powershell
npm ci
npm run build

cd backend\lexpro-backend
.\mvnw.cmd test
.\mvnw.cmd package -DskipTests
```

最后检查 `git status` 和 `git diff --cached`，确认没有密钥、大文件、数据库或真实案件数据后再推送。
